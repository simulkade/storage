function visualize_elect_data(begin_date, end_date)
    % visualizing the electricity production and demand between two dates
    d = extract_elec_data(begin_date, end_date);

    % visualize the supply and demand
    

end